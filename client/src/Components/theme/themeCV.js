import { createTheme } from '@material-ui/core/styles';

const customTheme = createTheme({
  palette: {
    primary: {
      main: '#1e88e5', // change primary color to blue
    },
  },
});

export default customTheme;